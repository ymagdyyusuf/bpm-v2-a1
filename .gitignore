# Environment variables (secrets — never commit)
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Supabase CLI local link cache (project ref, pooler URL — not secrets, but local-only)
supabase/.temp/

# Next.js build output
.next/
out/

# Misc
.DS_Store
*.log
