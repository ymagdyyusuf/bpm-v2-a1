# CLAUDE.md

> يُقرأ تلقائياً في كل جلسة **Code**.
> (تبويب **Cowork** بيقرا تعليمات البروجكت على claude.ai — نفس القواعد، مصدر تاني.)

---

## القواعد المُلزِمة — المخالفة تُرفَض بذكر رقمها

| # | القاعدة |
|---|---|
| **R-1** | الحقيقة في النظام الحي، لا في الوثيقة. الوثيقة تشرح **"ليه"** فقط |
| **R-2** | ممنوع ادعاء الحالة. لا `✅` ولا "مُنفَّذ" في أي ملف — الحالة تُقرأ بأمر. ما قدرتش؟ قول **"مش عارف"** |
| **R-3** | اللي مش مفهوم ما يتبنيش. ممنوع تخترع كيان/حالة/شاشة/حقل — الفجوة تُعلَن، وسؤال بخيارات، والشغل يقف |
| **R-4** | ترقيم واحد للأبد: `D-01` قرارات · `O-01` مفتوح · `A-01` ملحق. القرار = **سطر واحد** |
| **R-5** | الجلسة تنتهي بحاجة شغالة أو بسؤال واضح — لا بمستندات |
| **R-6** | الوثائق ست، لا سابع. أي مستند تاني = `docs/draft/` مؤقت |
| **R-7** | قرار يوسف في مجاله، وقرارك في التقنية. أي حاجة تخص شغله → **اسأل** |
| **R-8** | البوابة لا تُتخطّى — مخرج محدد + موافقة صريحة |
| **R-9** | كل قاعدة عمل لها اختبار، ولا شيء زيادة. التفاصيل: `.claude/reference/testing.md` |

## الأدوار — واحد في المرة، وتقوله في أول سطر: `[الدور: ...]`

| الدور | ممنوع عليه |
|---|---|
| `@analyst` | يقترح حل أو تقنية |
| `@architect` | **يكتب كود** |
| المصمّم | يغيّر منطق البيانات |
| الباني | يوسّع النطاق المتفَق عليه |
| `@reviewer` | يصلّح بنفسه، أو يمدح |

التفاصيل: `.claude/reference/roles.md`

---

## النظام

<جملة واحدة>
وصف المجال الكامل: `UNDERSTANDING.md` — **اقرأه قبل أي شغل على المنطق**.

## الروابط الحية — مصدر الحقيقة

<!-- غياب رابط من هنا = عطل. الرابط اللي مش هنا بيضيع. -->

| ماذا | أين |
|---|---|
| الموقع الحي | `<املأ بعد أول نشر>` |
| المستودع | `<املأ>` |
| قاعدة البيانات | `<املأ>` |
| لوحة النشر | `<املأ>` |
| لوحة التصميم | `<املأ — رابط Claude Design>` |

## الأوامر

```bash
npm run dev              # تشغيل محلي
npm run build            # بناء — لازم ينجح قبل أي commit
npx tsc --noEmit         # فحص الأنواع
supabase migration list  # المطبَّق فعلاً على القاعدة  ← مصدر الحقيقة
git log --oneline -10    # اللي اتعمل فعلاً        ← مصدر الحقيقة
```

## النشر

تلقائي من الفرع الرئيسي. أي `push` = نشر.
**ممنوع النشر اليدوي** — بيتنسي، فالمنشور بيفضل واقف على نسخة قديمة.

## المهاجرات

- كل تغيير في المخطط = **ملف جديد**. ممنوع تعديل ملف اتطبّق.
- `ADD COLUMN IF NOT EXISTS` دايماً.
- **"اتطبّقت" = ظاهرة في قائمة المهاجرات.** كتابة الملف ≠ تطبيق.
- **يوسف هو اللي يشغّلها على القاعدة الحية.**

## بنية المستودع

```
src/app/         شاشات — تعرض بس
src/components/  عناصر واجهة مشتركة
src/lib/         منطق الأعمال — لا JSX
src/lib/db/      الوصول لقاعدة البيانات
migrations/      تغييرات المخطط، مرقّمة
docs/draft/      مسودات مؤقتة
```

## معيار الكود

1. منطق الأعمال في `src/lib/` فقط.
2. **كل قاعدة عمل فوقها مصدرها** (`D-xx` أو `[قاله يوسف]`). منطق بلا مصدر = اختراع.
3. أي قايمة قيم ممكن تزيد = **جدول**، مش enum.
4. الربط بالمعرّف، مش بالاسم.
5. مفيش `any`. `tsc --noEmit && npm run build` قبل أي commit.
6. ملف عدّى ٣٠٠ سطر؟ غالباً بيعمل أكتر من حاجة.
7. كل ميزة تتشال من غير ما تكسر غيرها.
8. الفلترة في كل شاشات الاختيار حيّة وديناميكية وموثوقة: النتائج تضيق فوراً مع كل معيار (السنة · المرحلة · المادة · أو مجتمعة)، ولا تعرض خياراً لا يقابله سجل.

## نظام التصميم

مستخرَج من لوحة التصميم المعتمدة. الكود يُبنى من هذا النص لا من الصورة.

### الخطوط
```
العناوين : Cairo — 600 · 700 · 800
المتن    : IBM Plex Sans Arabic — 400 · 500 · 600 · 700
اللاتيني : IBM Plex Sans (احتياطي داخل النص العربي)
المصدر   : Google Fonts
```

### مقاس الخط
```
عنوان الصفحة      26px / 800   (شاشة التفصيل 24px / 800)
عنوان قسم         17px / 700
رأس الجدول        13px / 700 · بخط Cairo
خلية الجدول       15px / line-height 1.65
الشارة            12.5px / 600
تسمية الحقل       12px / 600
نص مساعد          13px – 13.5px
```

### الألوان — وضع فاتح واحد فقط
```
--bg           oklch(0.98 0.006 80)      أرضية الصفحة، أبيض دافئ
--surface      oklch(0.995 0.004 80)     أرضية البطاقات والجداول
--border       oklch(0.87 0.013 80)      حدود الحقول
--border-soft  oklch(0.925 0.009 80)     فواصل الصفوف وحدود البطاقات
--row-hover    oklch(0.975 0.007 80)     الصف تحت المؤشر
--text         oklch(0.26 0.02 260)      النص الأساسي
--text-muted   oklch(0.52 0.02 260)      النص الثانوي ورؤوس الجدول
--text-faint   oklch(0.55 0.015 260)     الأيقونات والنص المساعد
--accent       oklch(0.42 0.09 235)      اللون المميز — أزرق
--accent-strong oklch(0.35 0.09 235)     حالة التمرير على المميز
```

### ألوان الحالات (نصوصها مغمَّقة درجة عن اللوحة لراحة القراءة الطويلة)
```
لم يبدأ   خلفية oklch(0.93 0.006 260)  · نص oklch(0.40 0.012 260) · نقطة oklch(0.62 0.012 260)
جارٍ      خلفية oklch(0.945 0.05 78)   · نص oklch(0.38 0.095 68)  · نقطة oklch(0.68 0.13 70)
منتهية    خلفية oklch(0.935 0.045 150) · نص oklch(0.35 0.09 150)  · نقطة oklch(0.58 0.12 150)
```

### ألوان فئات المكوّن
```
كتاب   خلفية oklch(0.94 0.025 235) · نص oklch(0.35 0.08 235)
ملحق   خلفية oklch(0.95 0.01 80)   · نص oklch(0.40 0.018 80)
هدية   خلفية oklch(0.945 0.045 25) · نص oklch(0.40 0.1 25)
```

### الأشكال والمسافات
```
نصف قطر البطاقة   14px
نصف قطر الحقل     9px
نصف قطر الزر      10px
نصف قطر الشارة    999px (كبسولة)
حشو خلية الجدول   15px عمودي · 6px أفقي  → ارتفاع الصف ≈ 55px
حشو رأس الجدول    16px عمودي · 6px أفقي
حشو البطاقة       18px عمودي · 20px أفقي
حشو الصفحة        40px أعلى · 48px جانبي · 56px أسفل
حشو الشارة        5px عمودي · 13px أفقي
الفراغ بين الأقسام 22px – 24px
الفراغ داخل شريط الفلاتر 12px – 14px
```

### شبكة الجداول
```
قائمة الحزم : minmax(230px,1.8fr) 118px 118px 96px 108px 88px 82px 128px 118px
مكوّنات الحزمة : 104px 112px minmax(300px,2.3fr) 132px 88px 88px 96px 88px
```
عمود النص الطويل مرن، وبقية الأعمدة ثابتة العرض.

### قواعد مُلزِمة
- كل شاشة `dir="rtl" lang="ar"`
- `font-variant-numeric: tabular-nums` على كل عمود يحمل أرقاماً
- الأرقام تُعرض عربية (٠١٢٣٤٥٦٧٨٩) والفاصلة العشرية ٫ (U+066B)
- الأرقام العربية عرض فقط: القيم تُخزَّن وتُفلتَر وتُرتَّب بأرقام غربية، والتحويل عند العرض فقط
- دالة تحويل واحدة في `src/lib`، ولا تحويل يدوي داخل أي شاشة
- وضع فاتح واحد — لا وضع داكن. قرار مقصود لا سهو
- عمود السعر محاذاته لليسار، وأعمدة الأرقام الأخرى في المنتصف، وعمود النص الطويل لليمين
- النص الطويل يلتف على أكثر من سطر ولا يُقص أبداً
- المقاس يُعرض دائماً بصيغة «الطول × العرض» (مثال: ٢٨٫١ × ١٩٫٨)، أي page_height_cm ثم page_width_cm — بعكس ترتيب الحقول في قاعدة البيانات
- فاصل المقاس هو × بمسافة قبله وبعده

## ملاحظات تشغيلية

<!-- المصايد الحقيقية اللي اتعلمناها بالتجربة. زوّدها أول ما تتعلم واحدة. -->

- سكربتات الـhooks بـNode (`.mjs`) عشان تشتغل على Windows بلا git-bash.
